{
  "name": "cbprcslab-heat-transfer",
  "version": "0.1.0",
  "description": "Educational web tool for the heat transfer laboratory",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "start": "python3 -m http.server 8000",
    "test": "node --test tests/*.test.mjs",
    "export-reference": "node tools/export_reference.mjs",
    "verify": "npm run export-reference && uv run tools/verify_physics.py",
    "verify:python": "npm run export-reference && python3 tools/verify_physics.py"
  }
}
