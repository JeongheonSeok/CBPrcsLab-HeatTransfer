---
title: "열전달 실험 교육용 웹 도구 개발 계획"
subtitle: "핵심 구현 기능 중심 요약본"
version: "1.0 summary"
date: "2026-07-27"
language: "ko"
---

# 열전달 실험 교육용 웹 도구 개발 계획

## 실험 연계·Lumped-Parameter Model·사전 계산 CFD 중심 확장 요약본

---

## 1. 프로젝트 목적

본 프로젝트의 목적은 **실제 열전달 실험과 직접 연결되는 교육용 웹 도구**를 개발하는 것이다. 별도의 가상 실험으로 실제 실험을 대체하는 것이 아니라, 학생이 장치에서 수행한 조작과 측정값을 출발점으로 삼아 **실험 중 눈으로 볼 수 없었던 장치 내부의 현상**을 보여주는 보조 도구를 지향한다.

현재 실험에서는 학생이 장치 외부의 다이얼을 조절하고, 정상상태까지 기다린 뒤, 표시되는 전압·전류·유속·온도를 기록한다. 그러나 이 과정만으로는 다음 현상을 직접 관찰하기 어렵다.

- 수직 duct 내부에서 공기가 실제로 어느 방향으로 흐르는지
- 가열된 실린더 또는 관 벽 주변에서 온도 분포가 어떻게 형성되는지
- 자연대류 plume과 온도·속도 경계층이 어떻게 발달하는지
- 센서가 위치한 지점의 국소 온도와 전체 평균 온도가 어떻게 다른지
- 대류와 복사가 어느 표면에서 어느 정도의 열을 전달하는지
- 열전쌍이 공기 온도와 다른 값을 읽게 되는 과정

이 때문에 학생에게 실험장치가 하나의 black box처럼 느껴질 수 있다. 웹 도구는 이 black box를 교육적으로 열어 보여주는 역할을 한다. 학생이 실제로 사용한 전압, 유속, 센서 번호와 측정 온도를 웹 화면에 그대로 연결하고, 같은 조건에 대한 평균 모델과 CFD 결과를 함께 제시함으로써 **측정된 하나의 숫자가 장치 내부의 어느 위치와 어떤 물리 현상을 반영하는지** 이해하도록 돕는다.

따라서 시각화는 실험과 분리된 장식이 아니라 다음 과정을 연결하는 수단이어야 한다.

```text
실제 장치와 센서 위치 확인
→ 실제 실험 조건 및 측정값 입력
→ Lumped-parameter model로 평균 거동과 시간 변화를 확인
→ CFD로 실험 중 보지 못한 공간 분포를 확인
→ CFD에서 계산한 대류·복사 열전달량을 확인
→ 측정값·평균 모델·CFD를 같은 기준으로 비교
→ 차이의 원인과 모델 가정을 학생이 직접 검토
```

웹 도구는 다음 역할을 수행하도록 설계한다.

- 실제 장치와 동일한 센서 번호 및 위치를 사용하여 장치 구조를 설명한다.
- 학생이 수행한 전압·유속 조건과 측정값을 모델 및 CFD의 기준으로 사용한다.
- 실험에서 볼 수 없는 유체 흐름, 온도 분포, 열전달 경로를 시각화한다.
- 측정값을 교안 기반 계산, lumped-parameter model, CFD 결과와 연결한다.
- CFD 표면 결과로부터 대류 및 복사 열전달량을 계산하여 보여준다.
- 열전쌍의 크기, 복사율, 공기 유속에 따른 측정 오차를 비교한다.
- 각 결과에 적용된 가정, 경계조건, 적용 범위를 명확히 표시한다.

> **핵심 교육 원칙:** 실제 실험에서 출발하여, 실험 중 보지 못한 내부 유동·온도 분포·열전달 경로를 보여주고, 다시 실제 측정값의 해석으로 돌아오게 한다. 계산과 시각화는 도구가 지원하되 결과의 원인 분석과 보고서 논의는 학생에게 남겨 둔다.

---

## 2. 전체 개발 방향

웹 도구는 다음 요소를 결합한 **교육용 하이브리드 시뮬레이터**로 구성한다.

1. 실험장치와 센서 위치를 보여주는 인터랙티브 도식
2. 실험 A의 대류·복사 정상상태 계산 및 시각화
3. 실험 B의 열전쌍 측정 오차 계산 및 센서 비교
4. Lumped-parameter model을 이용한 시간별 평균 온도 변화 계산
5. 대표 조건에서 미리 계산한 CFD 결과의 유동장·온도장 뷰어
6. CFD 기반 대류·복사 열전달량 계산 및 비교
7. 학생 측정값, lumped model, CFD의 통합 비교 화면

### 2.1 CFD 개발의 기본 원칙

CFD는 웹 브라우저에서 실시간으로 새로 계산하지 않는다. 다음과 같은 **사전 계산·후처리·표시 방식**을 기본 방향으로 한다.

```text
연구실 계산 자원에서 대표 조건의 CFD 수행
→ 계산 결과의 수렴성과 물리적 타당성 검증
→ 교육에 필요한 field와 표면 열유속만 추출
→ 격자 해상도와 시간 frame 수를 줄여 요약
→ 수치 배열을 양자화·압축
→ case별 파일로 웹에 배포
→ 학생 브라우저에서는 선택한 결과를 읽어 시각화만 수행
```

이 방식의 장점은 다음과 같다.

- 별도의 유료 계산 서버가 필요하지 않다.
- 여러 학생이 동시에 접속해도 서버 계산 부하가 거의 증가하지 않는다.
- 실제 수업 전에 CFD 결과를 검증할 수 있다.
- 고해상도 원본 결과는 연구실에 보존하고, 웹에는 교육에 필요한 데이터만 배포할 수 있다.
- 학생의 PC는 데이터 표시, 그래프 생성, probe, 간단한 lumped model 계산에만 사용된다.

### 2.2 웹에서 직접 계산할 범위

브라우저에서 직접 계산하는 기능은 계산량이 작은 항목으로 제한한다.

- 교안 기반 정상상태 대류·복사 계산
- Lumped-parameter model의 시간 적분
- 열전쌍 정상상태 및 과도응답 계산
- 임계 유속 계산
- CFD 데이터의 평균값·표면 적분값 비교
- 그래프 생성, 데이터 보간, CSV 처리

브라우저에서 Navier–Stokes 방정식을 풀거나 새로운 CFD case를 생성하는 기능은 초기 및 중기 개발 범위에서 제외한다.

---

## 3. 운영 및 시스템 구성

웹사이트는 별도의 계산 서버와 데이터베이스가 필요 없는 **정적 웹 애플리케이션**으로 운영한다.

```text
무료 정적 웹 호스팅
├─ HTML / CSS / JavaScript
├─ 장치 도식과 설명 자료
├─ 교안 기반 계산 코드
├─ Lumped-parameter model 계산 코드
└─ 압축·요약된 CFD 데이터
              ↓
학생 브라우저
├─ 입력값 검증
├─ 정상상태 계산
├─ 시간 변화 계산
├─ CFD 결과 표시
├─ 표면 열전달량과 평균값 비교
├─ 그래프 및 애니메이션 출력
└─ 데이터의 로컬 저장 및 내보내기
```

### 운영 원칙

- 학생 계정과 중앙 데이터베이스를 두지 않는다.
- 학생이 입력한 값은 기본적으로 외부 서버에 전송하지 않는다.
- CFD 데이터는 사용자가 선택한 case만 필요할 때 내려받는다.
- GitHub Pages 또는 Cloudflare Pages와 같은 무료 정적 호스팅을 사용한다.
- 데스크톱과 노트북 사용을 우선하되 모바일에서도 기본 내용을 열람할 수 있게 한다.
- 고해상도 CFD 원본은 웹에 올리지 않고 연구실 내부에 보관한다.

### 권장 기술 수준

- 화면 및 일반 계산: JavaScript 또는 TypeScript
- 복잡한 UI 관리: React와 Vite
- 시간 적분: Web Worker
- 일반 그래프: Plotly.js 등
- 2D field 표시: Canvas
- 향후 3D 절단면: VTK.js 등 과학 시각화 도구
- 학생 데이터 및 CFD 캐시: 브라우저 내부 저장소

WebAssembly, Pyodide, WebGPU는 필수 요소로 두지 않는다. CFD는 사전 계산된 결과를 사용하므로, 초기 버전에서는 일반 웹 기술만으로 충분하다.

---

## 4. 권장 사이트 구성과 학습 흐름

상단 또는 좌측 메뉴는 다음과 같이 구성한다.

```text
[장치 이해] [실험 A] [실험 B] [시간 변화] [CFD 유동장] [비교 분석] [내 데이터] [모델과 가정]
```

| 화면 | 주요 목적 |
|---|---|
| 장치 이해 | 장치 구조, 센서 위치, 공기 흐름과 열전달 경로 확인 |
| 실험 A | 대류·복사 열전달량과 기여도 분석 |
| 실험 B | 열전쌍 측정 오차와 센서 특성 비교 |
| 시간 변화 | Lumped-parameter model을 이용한 가열·냉각 과정 확인 |
| CFD 유동장 | 사전 계산된 온도장·속도장과 표면 열유속 확인 |
| 비교 분석 | 측정값·lumped model·CFD 결과 비교 |
| 내 데이터 | 학생 측정값 입력, CSV 처리, 결과 저장 |
| 모델과 가정 | 사용한 모델, 경계조건, 적용 범위와 경고 확인 |

학생의 기본 사용 흐름은 다음과 같다.

```text
장치와 센서 위치 확인
→ 실제 실험 수행 및 측정값 입력
→ 교안 기반 정상상태 계산 확인
→ Lumped-parameter model로 시간별 평균 온도 확인
→ 같은 조건의 사전 계산 CFD case 선택
→ 유동장·온도장·대류/복사 표면 열유속 확인
→ 측정값·lumped model·CFD 비교
→ 모델 가정과 차이를 검토한 뒤 학생이 결과를 직접 논의
```

---

## 5. 핵심 구현 기능

## 5.1 실험장치 인터랙티브 도식

실험 A와 실험 B의 장치를 각각 단순화한 SVG 또는 Canvas 도식으로 제공한다. 실제 사진을 그대로 제시하기보다 학생이 구조를 이해하기 쉽도록 주요 구성만 강조한 교육용 그림을 사용한다.

### 실험 A 도식

- 수직 duct
- 수평 heated cylinder
- T8 공기 온도 센서
- T10 실린더 표면 온도 센서
- fan과 throttle plate
- 자연대류에 의한 상승 흐름
- 실린더에서 공기로 전달되는 대류
- 실린더에서 주변 표면으로 전달되는 복사

### 실험 B 도식

- 가열된 관 벽
- T5 유입 공기 온도 센서
- T6 작은 회색체 열전쌍
- T7 작은 흑체 열전쌍
- T8 큰 흑체 열전쌍
- T10 외벽 온도 센서
- fan과 공기 흐름
- 벽에서 열전쌍으로 전달되는 복사
- 열전쌍과 공기 사이의 대류
- 열전쌍 전선을 통한 전도 손실 가능성

### 상호작용

- 센서 또는 장치 구성요소를 클릭하면 해당 항목을 강조한다.
- 설명 패널에 측정 대상, 위치, 실험에서의 역할을 표시한다.
- 공기 흐름, 대류, 복사, 전도 표시를 각각 켜고 끌 수 있게 한다.
- 실험 A와 B에서 같은 센서 번호가 서로 다른 역할을 한다는 점을 명확히 구분한다.
- 이후 계산 및 CFD 화면의 센서 위치와 동일한 좌표 체계를 사용한다.

---

## 5.2 실험 A: 대류와 복사의 복합 열전달

학생이 실험에서 측정한 전압, 전류, T8, T10을 입력하면 교안의 정상상태 모델에 따라 대류와 복사 열전달량을 계산한다.

### 입력 항목

- 전압 $V$
- 전류 $I$
- 공기 온도 T8
- 실린더 표면 온도 T10
- 대류 모델 선택: Morgan 또는 McAdams
- Morgan 모델에 필요한 공기 물성
- 실린더 치수, 복사율, view factor

교안의 대표 조건인 5 V, 11 V, 17 V는 빠른 선택 버튼으로 제공하되, 실제 계산에는 학생이 측정한 전류와 온도값을 사용한다.

### 주요 결과

- 공급 전력 $Q_{\mathrm{in}}$
- 복사 열전달 계수와 복사 열전달량
- 대류 열전달 계수와 대류 열전달량
- 모델에서 계산된 총 열전달량
- 대류와 복사의 기여 비율
- Morgan과 McAdams 모델의 차이
- 공급 전력과 모델 열전달량의 차이

공급 전력과 계산된 대류·복사 열전달량의 차이는 단순히 `실험 오차`로 표시하지 않고 `에너지 수지 잔차` 또는 `모델에 포함되지 않은 열량`으로 표시한다.

### 시각화

- 실린더에 들어가는 전력과 빠져나가는 대류·복사 열량을 화살표 굵기로 표현한다.
- 대류와 복사의 비율을 막대 또는 도넛 그래프로 보여준다.
- 표면 온도에 따른 대류·복사 열전달량 변화를 그래프로 보여준다.
- 5 V, 11 V, 17 V 조건을 같은 축에서 비교한다.
- Morgan과 McAdams 계산 결과를 함께 표시한다.
- CFD에서 적분한 대류·복사 열전달량도 같은 그래프에 추가할 수 있게 한다.

---

## 5.3 실험 B: 온도 측정 시 복사 오차

실험 B 화면은 열전쌍이 실제 공기 온도와 다른 값을 읽는 이유를 벽 온도, 센서 특성, 공기 유속과 연결하여 보여준다.

### 입력 항목

- 외벽 온도 T10
- 유입 공기 온도 T5
- 공기 유속
- 센서 선택: T6, T7, T8
- 센서 비드 크기
- 센서 복사율
- view factor 또는 관련 가정
- 전선 전도 손실 포함 여부
- 허용 가능한 온도 측정 오차

### 대표 실험 조건

#### 자연대류

- fan off, 5 V
- fan off, 11 V
- fan off, 17 V

#### 강제대류

- 17 V, 0.2 m/s
- 17 V, 0.4 m/s
- 17 V, 0.8 m/s
- 17 V, 1.6 m/s

자연대류와 강제대류를 별도의 모드로 구분하고, 자연대류 조건을 생략하지 않는다.

### 주요 결과

- 예측 열전쌍 온도
- 실제 공기 온도와의 차이
- 벽에서 센서로 들어오는 복사 영향
- 센서와 공기 사이의 대류 영향
- 전도 손실을 포함한 확장 모델 결과
- T6, T7, T8 센서 간 비교
- 지정한 허용 오차를 만족하는 최소 유속

### 센서 비교

- T6와 T7: 같은 크기에서 복사율 차이
- T7과 T8: 유사한 표면 특성에서 비드 크기 차이
- 자연대류 5·11·17 V: 벽 온도 증가에 따른 복사 오차 변화
- 강제대류 0.2·0.4·0.8·1.6 m/s: 유속 증가에 따른 대류 냉각 효과

### 시각화

- 가열된 벽, 공기, 열전쌍 사이의 열전달 경로를 표시한다.
- 복사 유입과 대류 냉각의 상대적 크기를 막대로 보여준다.
- 자연대류 조건에서는 전압별 T6·T7·T8 온도를 비교한다.
- 강제대류 조건에서는 유속별 T6·T7·T8 온도를 비교한다.
- 공기 온도와 센서 온도 사이의 차이를 별도 오차 그래프로 표시한다.
- CFD에서 추출한 센서 위치의 국소 온도와 속도를 입력으로 사용한 결과도 비교한다.

교안에서 정확한 값이 주어지지 않은 복사율, 전선 전도 계수, 일부 view factor는 숨은 기본값으로 처리하지 않는다. 결과에 `사용자 가정` 또는 `확장 모델` 표시를 붙인다.

---

## 5.4 Lumped-Parameter Model을 이용한 시간별 온도 변화

정상상태 결과만으로는 학생이 왜 기다려야 하는지, 가열·냉각 과정에서 각 물체의 온도가 어떻게 변하는지 이해하기 어렵다. 따라서 장치의 주요 부분을 소수의 평균 온도 노드로 나타내는 **lumped-parameter model**을 별도 화면에서 계산한다.

일반적인 형태는 다음과 같다.

$$
C_i\frac{dT_i}{dt} = \sum Q_{\mathrm{in},i} - \sum Q_{\mathrm{out},i}
$$

브라우저에서는 이 연립 1차 미분방정식을 시간에 따라 적분하여, 실험 시작부터 정상상태까지의 평균 온도 변화를 보여준다.

### 실험 A

- 실린더·히터 평균 표면 온도 $T_s(t)$
- 측정된 T8을 외부 입력으로 사용하는 1-node 모델
- 필요 시 duct 공기 평균 온도 $T_a(t)$를 포함한 2-node 모델
- 전압 또는 입력 전력 변화에 따른 가열·냉각 응답
- 시간별 대류 열전달량 $Q_c(t)$
- 시간별 복사 열전달량 $Q_r(t)$
- 추가 열손실을 포함한 확장 모델
- 정상상태 온도와 정상상태 도달 예상 시간

### 실험 B

열전쌍의 시간 응답은 다음과 같은 형태로 계산한다.

$$
C_c\frac{dT_c}{dt} = Q_{\mathrm{rad}} - Q_{\mathrm{conv}} - Q_{\mathrm{cond}}
$$

- T6, T7, T8의 시간별 센서 온도
- 벽 온도 T10과 공기 온도 T5의 시간 기록 사용
- fan on/off 또는 유속 변경 이후의 과도응답
- 자연대류에서 전압 변화에 따른 센서 응답
- 강제대류에서 유속 변화에 따른 센서 응답
- 각 시간 단계의 복사·대류·전도 기여도
- 센서 크기와 복사율에 따른 시간 지연 및 오차 비교

### 화면에서 제공할 결과

- 시간에 따른 각 노드의 평균 온도 곡선
- 실제 시간별 측정 데이터와 모델 곡선의 중첩 비교
- 전압 또는 유속을 변경한 시점
- 시간별 대류·복사·전도 열전달량
- 정상상태 판정 시점과 판정 기준
- 가열 속도, 냉각 속도, 센서 시간 지연

### 모델 수준 구분

- `교안 기반 모드`: 교안의 기본 열전달 관계를 사용한 교육용 모델
- `보정 모델 모드`: 실제 시간 기록으로 유효 열용량과 추가 열손실 계수를 추정한 모델
- `교육용 가상 조건`: 파라미터 변화의 경향을 확인하는 모델

교안에는 정상상태 열수지와 정상상태 판정 절차가 제시되어 있지만, 시간 응답을 정량적으로 재현하는 데 필요한 유효 열용량과 추가 열손실 계수는 충분히 주어지지 않는다. 따라서 실제 장치의 시간 응답과 비교하려면 추가 시간 기록을 이용한 보정이 필요하다.

---

## 5.5 사전 계산 CFD 결과 뷰어

CFD 기능의 목적은 실험에서 직접 볼 수 없는 공간적인 유동, 온도 분포, 국소 열유속을 보여주는 것이다. 웹사이트는 CFD를 새로 계산하지 않고, 연구실에서 미리 계산하고 검증한 결과를 읽어 표시한다.

### 5.5.1 우선 계산할 대표 case

#### 실험 A: 자연대류

- 5 V 대표 조건
- 11 V 대표 조건
- 17 V 대표 조건

각 case는 가능한 한 해당 실험에서 측정한 T8, T10, 초기 온도, 주변 조건과 연결한다.

#### 실험 B: 자연대류

- fan off, 5 V
- fan off, 11 V
- fan off, 17 V

#### 실험 B: 강제대류

- 17 V, 0.2 m/s
- 17 V, 0.4 m/s
- 17 V, 0.8 m/s
- 17 V, 1.6 m/s

따라서 1차 CFD 데이터셋은 최소한 다음 10개 정상상태 case를 포함하는 것을 권장한다.

```text
실험 A 자연대류: 3 cases
실험 B 자연대류: 3 cases
실험 B 강제대류: 4 cases
합계: 10 cases
```

필요한 경우 실제 반복 실험의 평균 조건 또는 저온·중간·고온 대표 조건을 추가한다.

### 5.5.2 웹에서 표시할 CFD 물리량

- 온도장 $T$
- 속도 성분 $u$, $v$ 및 속도 크기
- 고체와 유체 영역 mask
- 압력 또는 압력차: 교육적으로 필요한 경우에만
- 센서 위치와 probe 값
- 가열 표면의 국소 대류 열유속 $q''_{\mathrm{conv}}$
- 가열 표면의 국소 복사 열유속 $q''_{\mathrm{rad}}$
- 표면 적분으로 계산한 총 대류·복사 열전달량

### 5.5.3 CFD 결과로부터 대류·복사 열전달량 계산

CFD 후처리 단계에서 가열 표면의 국소 열유속을 적분하여 총 열전달량을 계산한다.

$$
Q_{\mathrm{conv}}^{\mathrm{CFD}}(t) = \int_A q''_{\mathrm{conv}}(\mathbf{x},t)\,dA
$$

$$
Q_{\mathrm{rad}}^{\mathrm{CFD}}(t) = \int_A q''_{\mathrm{rad}}(\mathbf{x},t)\,dA
$$

$$
Q_{\mathrm{tot}}^{\mathrm{CFD}}(t) = Q_{\mathrm{conv}}^{\mathrm{CFD}}(t) + Q_{\mathrm{rad}}^{\mathrm{CFD}}(t)
$$

웹 화면에서는 다음 값을 표시한다.

- CFD 대류 열전달량
- CFD 복사 열전달량
- CFD 총 열전달량
- 대류·복사 기여 비율
- 평균 대류 열전달 계수
- 표면 위치별 국소 열유속 분포
- 공급 전력 또는 입력 열유속과의 에너지 수지

#### 복사 계산 방식의 구분

복사 열전달량은 CFD 해석 설정에 따라 두 방식으로 얻을 수 있다.

1. **CFD 직접 결합 방식**  
   CFD solver에서 radiation model을 활성화하고, 표면의 net radiative heat flux를 직접 출력하여 적분한다.

2. **CFD 기반 후처리 방식**  
   CFD에서 얻은 표면 온도와 주변 표면 온도를 사용하여 별도의 view-factor 또는 radiation 계산으로 복사 열유속을 구한다.

두 방식은 화면에서 다음처럼 구분한다.

- `CFD 직접 계산: 복사 모델 결합`
- `CFD 온도장 + 복사 후처리`

복사를 CFD에 포함하지 않았는데도 결과를 `CFD 복사 열전달량`으로 단순 표기해서는 안 된다. 사용한 복사 모델과 view factor, 표면 복사율을 case metadata에 기록한다.

### 5.5.4 실험별 열전달량 표시

#### 실험 A

가열 실린더 표면을 기준으로 다음 값을 계산하고 비교한다.

- $Q_{\mathrm{conv}}^{\mathrm{CFD}}$
- $Q_{\mathrm{rad}}^{\mathrm{CFD}}$
- $Q_{\mathrm{tot}}^{\mathrm{CFD}}$
- 교안 기반 Morgan·McAdams 결과
- Lumped-parameter model의 $Q_c(t)$와 $Q_r(t)$
- 실험 공급 전력 $VI$

이를 통해 학생은 평균 상관식과 CFD 표면 적분 결과가 왜 다를 수 있는지 확인할 수 있다.

#### 실험 B

초기 CFD에서는 전체 duct와 가열 벽의 유동·온도장을 계산하고 다음을 제공한다.

- 가열 벽에서 공기로 전달되는 대류 열전달량
- 가열 벽의 복사 열전달량
- T6, T7, T8 위치의 국소 공기 온도와 속도

열전쌍 비드를 CFD 격자에 직접 포함하지 않는 초기 버전에서는 센서별 복사·대류 열전달량을 다음과 같이 계산한다.

```text
CFD에서 센서 위치의 국소 공기 온도·속도 추출
→ 센서별 lumped 열수지 모델에 입력
→ 센서의 Qconv, Qrad, Qcond와 Tc 계산
→ 실제 T6, T7, T8 측정값과 비교
```

이 결과는 `CFD field + 센서 lumped model`로 표시한다. 향후 필요성이 확인되면 대표 case 한두 개에 대해 열전쌍 비드를 직접 포함한 고해상도 CFD를 수행하여 결합 모델을 검증할 수 있다.

### 5.5.5 Lumped-Parameter Model과 CFD 비교

두 모델은 서로 대체 관계가 아니라 같은 실험을 서로 다른 해상도로 설명한다.

| 모델 | 주요 역할 |
|---|---|
| Lumped-parameter model | 평균 온도, 열수지, 시간 응답을 빠르게 계산 |
| CFD | 공간별 온도·속도 분포와 국소·표면 열유속을 계산 |

동일한 실험 조건에서 다음 항목을 비교한다.

- 실린더 또는 가열 벽의 평균 온도
- duct 내부 공기의 체적 평균 온도
- T5, T6, T7, T8 위치의 국소 온도와 속도
- 대류 열전달량
- 복사 열전달량
- 대류·복사 기여 비율
- 정상상태 값
- 대표 transient case의 정상상태 도달 시간

비교 화면에서는 다음 결과를 같은 그래프 또는 표에 표시한다.

```text
실험 측정값
교안 기반 계산
Lumped-parameter model
CFD 표면 적분 및 공간 평균
```

#### 경계조건에 따른 비교상의 주의

측정된 T10을 CFD의 표면온도 경계조건으로 직접 사용한 경우, CFD의 T10은 독립적인 예측값이 아니다. 이 경우에는 T10의 일치 여부를 검증 결과로 사용하지 않고 다음 값을 비교한다.

- 센서 위치의 공기 온도
- 유동 구조와 속도
- 표면 대류 열유속
- 평균 대류 열전달 계수
- 복사 열유속과 총 열전달량

표면 온도 $T_s(t)$ 자체를 실험값 및 lumped model과 직접 비교하려면, 별도의 transient CFD에서 측정 T10을 강제하지 않고 입력 전력 또는 등가 열유속을 경계조건으로 사용해야 한다.

### 5.5.6 CFD 화면 기능

- 실험 A 또는 B 선택
- 자연대류 또는 강제대류 모드 선택
- 조건별 CFD case 선택
- 온도, 속도, 대류 열유속, 복사 열유속 중 표시 물성 선택
- 속도 화살표 표시 켜기·끄기
- 가열 표면의 국소 열유속 그래프 표시
- 총 $Q_{\mathrm{conv}}$, $Q_{\mathrm{rad}}$, $Q_{\mathrm{tot}}$ 표시
- 색상 범위를 자동, 공통, 사용자 지정으로 전환
- 주요 단면 선택
- 마우스로 위치별 온도와 속도 확인
- T5, T6, T7, T8, T10 위치로 바로 이동
- 두 case를 같은 색 범위와 같은 화살표 크기로 비교
- 경계조건과 모델 가정 패널 확인

---

## 5.6 CFD 데이터 요약·압축 및 배포 방식

웹에는 CFD solver의 전체 원본 파일을 올리지 않는다. 교육적 목적에 필요한 값만 추출하고, 해상도를 낮추어 별도 웹 데이터셋으로 만든다.

### 후처리 과정

1. 원본 CFD 결과의 수렴성과 질량·에너지 수지를 확인한다.
2. 교육에 필요한 2D 단면 또는 coarse 3D grid를 선택한다.
3. 온도와 속도 field를 목표 격자로 downsampling한다.
4. 가열 표면의 대류·복사 열유속을 추출한다.
5. 표면 열유속을 적분하여 case별 총 열전달량을 계산한다.
6. 센서 위치와 주요 probe 값을 별도 표로 저장한다.
7. transient case는 대표 시간 frame만 선택한다.
8. 수치 배열을 16-bit 수준으로 양자화한다.
9. gzip 또는 Brotli 압축이 잘 적용되는 이진 형식으로 저장한다.
10. 경계조건, 가정, 단위, 양자화 오차를 metadata에 기록한다.

### 웹에 저장할 데이터

- 온도장과 속도장
- 표면 대류·복사 열유속 분포
- 총 대류·복사 열전달량
- 공간 평균 온도
- 센서 위치 probe 값
- 대표 시간 frame
- 고체·유체 영역 mask
- case의 경계조건과 모델 가정
- 원본 격자와 웹 격자의 해상도
- downsampling 및 양자화 오차

### 데이터 크기 관리 원칙

- 첫 버전은 정상상태 2D 단면을 중심으로 구성한다.
- 각 case는 가능한 한 수 MB 수준으로 유지한다.
- transient 결과는 모든 time step이 아니라 20~60개의 대표 frame만 저장한다.
- frame 사이 보간은 시각적 표현에만 사용하며, 보간 frame을 실제 CFD 계산값으로 표시하지 않는다.
- case별 파일을 분리하고 사용자가 선택한 파일만 불러온다.
- 전체 3D 고해상도 원본은 웹에 배포하지 않는다.

### 표시 문구 예시

```text
이 화면은 연구실에서 미리 계산한 CFD 결과를
교육용으로 downsampling·양자화한 데이터입니다.

표시 해상도는 원본 CFD 격자보다 낮으며,
그래프의 총 열전달량은 원본 또는 검증된 후처리 격자에서 계산한 값입니다.
```

가능하면 표면 적분 열전달량은 지나치게 낮춘 표시 격자에서 다시 계산하지 않고, 원본 CFD 또는 충분히 세밀한 후처리 데이터에서 계산한 값을 함께 저장한다.

---

## 5.7 학생 실험 데이터 입력 및 통합 비교

학생이 실험값을 사이트에 직접 입력하거나 CSV 파일로 불러올 수 있도록 한다.

### 실험 A 데이터

- Data ID
- 전압
- 전류
- T8
- T10
- 시간 데이터가 있는 경우 time column

### 실험 B 데이터

- Data ID
- 전압
- 공기 유속
- T5
- T6
- T7
- T8
- T10
- 자연대류/강제대류 구분
- 시간 데이터가 있는 경우 time column

### 제공 기능

- 표에서 직접 입력 및 수정
- CSV 불러오기와 내보내기
- 숫자가 아닌 값, 누락값, 단위 오류 확인
- 교안 기반 계산 결과 자동 생성
- 측정 조건과 가장 가까운 CFD case 제안
- 측정값, lumped model, CFD 결과를 구분하여 표시
- 시간 데이터가 있으면 측정 온도와 lumped model을 같은 시간축에서 비교
- 대표 transient CFD가 있으면 같은 시간축에 추가
- 정상상태에서는 온도, 열전달량, 기여 비율을 공통 표로 비교
- 현재 입력값을 브라우저 내부에 임시 저장

### 비교표 예시

| 비교 항목 | 실험값 | 교안 계산 | Lumped model | CFD |
|---|---:|---:|---:|---:|
| 표면 또는 벽 온도 |  |  |  |  |
| 공기 평균 온도 |  |  |  |  |
| 센서 위치 온도 |  |  |  |  |
| 대류 열전달량 |  |  |  |  |
| 복사 열전달량 |  |  |  |  |
| 대류 기여율 |  |  |  |  |
| 복사 기여율 |  |  |  |  |
| 정상상태 도달 시간 |  |  |  |  |

결과의 원인 서술, 실험 오차의 자동 확정, 보고서 문장 자동 생성, Open question 답안 생성은 제외한다.

---

## 5.8 모델, 가정 및 적용 범위 표시

모든 결과에는 결과의 성격을 알 수 있는 배지를 붙인다.

- `측정값`
- `교안 기반 계산`
- `Lumped model`
- `보정된 모델`
- `CFD 직접 계산`
- `CFD + 복사 후처리`
- `CFD field + 센서 lumped model`
- `Downsampled CFD 표시`
- `보간 frame`
- `사용자 가정`
- `적용 범위 외`

각 CFD case에는 최소한 다음 정보를 함께 표시한다.

- 2D 또는 3D 여부
- 정상상태 또는 비정상상태 여부
- 층류 또는 난류 모델
- Boussinesq 근사 사용 여부
- 온도·속도·열유속 경계조건
- 복사 모델과 view factor 처리 방식
- 센서 비드를 격자에 포함했는지 여부
- 원본 격자와 웹 표시 격자
- downsampling 및 양자화 방식
- 계산된 frame과 화면 보간 frame의 구분
- solver와 후처리 버전

### 경고가 필요한 경우

- 상관식의 적용 범위를 벗어난 경우
- 교안에 없는 물성값이나 복사율을 사용한 경우
- CFD case와 학생 측정 조건의 차이가 큰 경우
- 정상상태 가정을 만족하지 않은 데이터를 입력한 경우
- 센서의 정확한 위치나 장치 치수가 확인되지 않은 경우
- lumped model의 파라미터가 실제 장치에서 보정되지 않은 경우
- radiation model이 CFD에 직접 결합되지 않은 경우
- CFD 표시 데이터의 압축 오차가 설정 기준을 넘는 경우

---

## 6. UI 및 사용자 경험 설계

### 기본 레이아웃

- 왼쪽: 입력 조건과 모델 선택
- 중앙: 장치 도식, 에너지 흐름 또는 CFD 화면
- 오른쪽: 핵심 계산 결과, 대류·복사 열전달량, 경고
- 하단: 조건 비교 그래프, 표, 생각해보기 질문

### 디자인 원칙

- 흰색 또는 밝은 회색 배경을 사용한다.
- 강조색은 2~3개 이내로 제한한다.
- 대류, 복사, 측정값, lumped model, CFD의 색을 사이트 전체에서 일관되게 사용한다.
- 제목, 본문, 보조 설명의 글자 크기 차이를 과도하게 두지 않는다.
- 단위는 입력값과 결과값에 항상 함께 표시한다.
- 색만으로 정보를 구분하지 않고 선 종류, 마커, 텍스트 배지를 함께 사용한다.

### CFD 화면의 핵심 패널

```text
[Case 조건]
[온도/속도/대류 열유속/복사 열유속 선택]
[유동장 화면]
[표면 열유속 분포]
[Qconv / Qrad / Qtotal / 기여율]
[실험·Lumped·CFD 비교]
[가정 및 압축 정보]
```

### 비교의 공정성

- 두 case를 비교할 때 온도 색 범위를 공통으로 고정할 수 있게 한다.
- 속도 화살표 scale을 화면에 표시한다.
- 열유속 분포는 동일한 단위와 축 범위를 사용한다.
- downsampling된 field와 원본에서 적분한 총 열전달량을 구분한다.

---

## 7. 검증 계획

## 7.1 교안 기반 및 Lumped Model 검증

- 웹 계산을 독립적인 Python 계산 또는 수기 계산과 비교한다.
- 단위 변환과 절대온도 사용을 확인한다.
- Morgan과 McAdams 모델의 선택 및 적용 범위를 확인한다.
- 시간 간격을 줄였을 때 lumped model 결과가 수렴하는지 확인한다.
- 한 조건에서 보정한 파라미터를 다른 조건에서 검증한다.

## 7.2 CFD 검증

- coarse, medium, fine mesh 결과를 비교한다.
- 정상상태 수렴 여부와 질량수지를 확인한다.
- 전체 에너지 수지와 표면 열유속 적분값을 확인한다.
- 대류 열유속과 복사 열유속의 부호 및 단위를 확인한다.
- 복사 모델을 사용하는 경우 view factor, 복사율, 주변 표면 조건을 검토한다.
- 센서 위치의 CFD 온도와 실험 측정값을 비교한다.
- 측정값을 경계조건으로 사용한 물리량은 독립적인 검증 항목에서 제외한다.

## 7.3 CFD 열전달량 검증

다음 관계를 case별로 확인한다.

$$
Q_{\mathrm{input}} \approx Q_{\mathrm{conv}}^{\mathrm{CFD}} + Q_{\mathrm{rad}}^{\mathrm{CFD}} + Q_{\mathrm{other}}^{\mathrm{CFD}} + \frac{dE}{dt}
$$

정상상태에서는 축적항 $dE/dt$가 충분히 작아야 한다. $Q_{\mathrm{other}}$에는 지지대 전도, 외부 표면 손실, 지정한 추가 경계조건 등이 포함될 수 있다.

- 표면별 열유속의 적분 방향을 확인한다.
- CFD solver가 제공하는 heat balance report와 독립 후처리 적분을 비교한다.
- downsampling 전후의 field 오차를 확인한다.
- 총 열전달량은 가능한 한 원본 또는 충분히 세밀한 격자에서 계산한다.

## 7.4 모델 간 비교 검증

- 같은 초기조건과 경계조건에서 lumped model과 CFD를 비교한다.
- CFD의 공간 평균값을 lumped model의 평균 온도와 비교한다.
- 대류·복사 열전달량을 공통 단위와 동일 표면 기준으로 비교한다.
- 시간 비교에서는 동일한 시작 시점과 경계조건을 사용한다.
- 두 모델의 차이를 단순 오차로 처리하지 않고 공간 균일 온도 가정, 2D 근사, 경계조건 차이로 해석한다.

## 7.5 압축·요약 데이터 검증

- 원본 field와 downsampled field의 최대·평균 오차를 계산한다.
- 센서 위치 probe 값이 허용 오차 안에서 유지되는지 확인한다.
- 표면 열유속 분포의 주요 peak 위치가 보존되는지 확인한다.
- 양자화 후 온도와 속도 오차를 metadata에 기록한다.
- 압축된 데이터로 다시 계산한 시각적 평균값과 원본 후처리 값을 비교한다.

## 7.6 사용성 검증

- 학생이 실험 A와 B의 센서 번호를 혼동하지 않는지 확인한다.
- 자연대류와 강제대류 모드를 구분할 수 있는지 확인한다.
- 측정값, lumped model, CFD 결과를 구분할 수 있는지 확인한다.
- 대류·복사 열전달량의 출처를 이해할 수 있는지 확인한다.
- 저사양 노트북에서도 field 표시와 그래프가 무리 없이 동작하는지 확인한다.

---

## 8. 기능 우선순위

| 기능 | 교육 효과 | 구현 난이도 | 우선순위 |
|---|---:|---:|---:|
| 실험장치와 센서 인터랙티브 도식 | 매우 높음 | 낮음 | 1 |
| 실험 A 정상상태 계산 및 그래프 | 높음 | 낮음 | 1 |
| 실험 B 자연·강제대류 센서 오차 계산 | 매우 높음 | 중간 | 1 |
| 학생 데이터 입력 및 비교 | 높음 | 낮음 | 1 |
| 모델 가정 및 적용 범위 표시 | 매우 높음 | 낮음 | 1 |
| 실험 A 평균 온도 시간 모델 | 높음 | 중간 | 2 |
| 실험 B 열전쌍 시간 응답 모델 | 높음 | 중간 | 2 |
| 실험 A 사전 계산 CFD 3 cases | 매우 높음 | 중간 | 2 |
| 실험 B 자연대류 CFD 3 cases | 매우 높음 | 중간 | 2 |
| 실험 B 강제대류 CFD 4 cases | 매우 높음 | 중간 | 2 |
| CFD 대류·복사 표면 열유속 적분 | 매우 높음 | 중간 | 2 |
| CFD와 lumped model 비교 | 매우 높음 | 중간 | 2 |
| CFD downsampling·압축 파이프라인 | 높음 | 중간 | 2 |
| 대표 transient CFD 애니메이션 | 중간 | 높음 | 3 |
| coarse 3D 절단면 | 중간 | 높음 | 3 |
| 열전쌍 비드를 직접 포함한 고해상도 CFD | 중간 | 매우 높음 | 4 |
| 브라우저 내 실시간 CFD 계산 | 낮은 비용효율 | 매우 높음 | 제외 |

---

## 9. 단계별 개발 계획

## 9.1 단계 0: 모델·기하·데이터 정리

### 주요 작업

- 실험 A와 B의 입력값, 출력값, 단위를 정리한다.
- 센서 이름과 실제 좌표를 확정한다.
- 교안값, 측정값, 추정값, 사용자 가정을 구분한다.
- duct, 실린더, 가열 벽의 치수를 확인한다.
- 실제 정상상태 및 시간 데이터 한 세트 이상을 확보한다.
- CFD의 표면 이름과 열수지 기준을 정한다.

### 완료 기준

- 기능별 입력·출력 목록이 문서화되어 있다.
- 실험 B의 자연대류와 강제대류 조건이 모두 case 목록에 포함되어 있다.
- CFD에서 적분할 대류·복사 표면이 명확히 정의되어 있다.

## 9.2 단계 1: 기본 웹 MVP

### 포함 기능

- 실험 A/B 장치 인터랙티브 도식
- 실험 A 정상상태 대류·복사 계산
- Morgan과 McAdams 모델 비교
- 실험 B 자연·강제대류 센서 오차 계산
- 기본 비교 그래프
- 모델과 가정 패널
- 정적 웹 호스팅 배포

### 완료 기준

- 학생에게 링크를 공유해 바로 사용할 수 있다.
- 계산은 서버 없이 브라우저에서 수행된다.
- 각 결과에 단위와 모델 종류가 표시된다.
- 자연대류 5·11·17 V와 강제대류 유속 조건을 선택할 수 있다.

## 9.3 단계 2: 시간 변화 및 학생 데이터

### 포함 기능

- CSV 입력과 내보내기
- 측정값과 모델값 비교
- 실린더 평균 온도 시간 변화
- 열전쌍 시간 응답
- 정상상태 도달 시간
- 임계 유속 계산
- 브라우저 내부 임시 저장
- 보정 모델과 교육용 모델 구분

### 완료 기준

- 실제 시간 데이터와 lumped model 결과를 같은 그래프에서 비교할 수 있다.
- 각 시간 단계에서 대류·복사·전도 기여도를 확인할 수 있다.
- 보정되지 않은 파라미터는 명확히 표시된다.

## 9.4 단계 3: 대표 CFD 계산 및 후처리

### 우선 CFD 계산

- 실험 A 자연대류 5·11·17 V
- 실험 B 자연대류 5·11·17 V
- 실험 B 강제대류 17 V, 0.2·0.4·0.8·1.6 m/s

### 후처리 항목

- 온도장과 속도장 추출
- 센서 위치 probe 값 추출
- 표면 대류 열유속 추출 및 적분
- 표면 복사 열유속 추출 및 적분
- 공간 평균 온도 계산
- case별 에너지 수지 작성
- 원본 결과 검증

### 완료 기준

- 각 case의 $Q_{\mathrm{conv}}^{\mathrm{CFD}}$, $Q_{\mathrm{rad}}^{\mathrm{CFD}}$, $Q_{\mathrm{tot}}^{\mathrm{CFD}}$가 정리되어 있다.
- 자연대류와 강제대류 조건을 모두 비교할 수 있다.
- CFD solver report와 독립 표면 적분값이 허용 범위 안에서 일치한다.
- 각 case에 경계조건과 복사 처리 방식이 기록되어 있다.

## 9.5 단계 4: CFD 웹 데이터 변환 및 뷰어 연결

### 포함 기능

- 2D field downsampling
- 수치 배열 양자화 및 압축
- case별 metadata 생성
- 온도장·속도장·열유속 field 표시
- 속도 화살표 및 probe
- 표면 열유속 분포 그래프
- 총 대류·복사 열전달량 패널
- 조건별 case 전환과 lazy loading
- 측정값·lumped model·CFD 비교

### 완료 기준

- 10개 대표 정상상태 case가 웹에서 선택 가능하다.
- 선택하지 않은 case는 다운로드하지 않는다.
- 센서 위치의 국소 온도와 속도를 확인할 수 있다.
- CFD에서 계산한 대류·복사 열전달량을 같은 화면에서 확인할 수 있다.
- 원본과 웹 데이터의 압축 오차가 기록되어 있다.

## 9.6 단계 5: 선택적 고급 기능

- 대표 case 한두 개의 transient CFD
- sparse time frame 애니메이션
- coarse 3D 데이터와 주요 절단면
- 열전쌍 비드를 직접 포함한 검증용 고해상도 CFD
- 오프라인 사용 기능
- 조교용 case 관리 도구

이 단계에서도 CFD는 사전 계산 방식으로 유지한다. 브라우저가 새로운 CFD를 생성하는 기능은 개발하지 않는다.

---

## 10. 첫 공개 버전의 권장 범위

첫 공개 버전은 다음 기능에 집중하는 것이 적절하다.

1. 실험 A/B 장치와 센서 위치를 설명하는 인터랙티브 도식
2. 실험 A의 대류·복사 열전달량 계산
3. Morgan과 McAdams 모델 비교
4. 실험 B의 자연대류 5·11·17 V 센서 비교
5. 실험 B의 강제대류 0.2·0.4·0.8·1.6 m/s 센서 비교
6. Lumped-parameter model의 시간별 온도 변화
7. 학생 실험값 입력 및 CSV 내보내기
8. 모델과 가정, 적용 범위 경고
9. 실험 A 정상상태 CFD 3 cases
10. 실험 B 자연대류 CFD 3 cases
11. 실험 B 강제대류 CFD 4 cases
12. CFD 온도장·속도장·표면 열유속 뷰어
13. CFD 기반 대류·복사 총 열전달량 표시
14. 측정값·교안 계산·lumped model·CFD 비교
15. 무료 정적 호스팅과 브라우저 내 경량 계산

초기 범위에서 제외할 기능은 다음과 같다.

- 웹에서 실시간으로 새로운 CFD를 계산하는 기능
- 사용자가 임의의 기하와 경계조건을 입력해 CFD를 생성하는 기능
- 전체 고해상도 3D transient 원본의 웹 배포
- 사용자 계정과 서버 데이터베이스
- 결과보고서 또는 논의문 자동 생성
- 검증되지 않은 파라미터의 숨은 기본값

---

## 11. 추가로 확보할 자료

### 우선순위가 높은 자료

- 실험 A에서 가열 시작부터 정상상태까지의 T8, T10 시간 기록
- 실험 B 자연대류 5·11·17 V에서 T5~T10 시간 기록
- 실험 B 강제대류에서 fan on 및 유속 변경 전후 T5~T10 시간 기록
- 각 조건의 전압과 전류 시간 기록
- duct, 실린더, 가열 벽의 실제 치수
- 센서의 정확한 위치 좌표
- CFD 경계조건에 사용할 주변 온도와 입구 조건

### 모델 정교화에 필요한 자료

- 실린더와 히터의 질량 및 열용량
- T6의 실제 재질과 복사율
- T7, T8의 표면 특성
- 열전쌍 전선의 직경, 길이, 열전도도
- 장치 외벽과 주변 표면 온도
- fan 설정과 실제 유속의 관계
- 동일 조건에서의 반복 측정 데이터
- 지지대 및 장치 외부로의 추가 열손실 정보

가장 먼저 확보할 자료는 **가열 시작부터 정상상태까지의 온도 시간 기록**과 **장치의 실제 기하·센서 좌표**이다. 전자는 lumped model의 보정에, 후자는 CFD 기하와 probe 위치의 정의에 직접 필요하다.

---

## 12. 프로젝트의 최종 지향점

이 웹 도구는 단순히 계산 결과를 대신 만들어주는 사이트가 아니라 다음과 같은 역할을 수행해야 한다.

> **실제 실험에서 출발하여, 장치 내부에서 눈으로 볼 수 없었던 유동·온도 분포·국소 열유속을 시각화하고, CFD 결과에서 대류와 복사 열전달량을 정량화하며, 이를 측정값과 lumped-parameter model에 연결한 뒤 다시 실험 결과의 해석으로 돌아오게 하는 교육용 플랫폼**

권장 개발 순서는 다음과 같다.

```text
실제 장치와 센서 이해
→ 실제 측정값 연결
→ 정상상태 열수지 확인
→ Lumped-parameter model로 시간 변화 확인
→ 대표 조건의 CFD를 연구실에서 사전 계산
→ CFD 결과에서 대류·복사 열유속과 총 열전달량 후처리
→ 결과를 downsampling·압축하여 웹에 배포
→ 유동장·열전달량·측정값·lumped model 통합 비교
→ 교육적 필요성이 확인된 경우에만 사전 계산 3D·transient 결과 확장
```

이 방향을 따르면 무료 정적 웹 호스팅만으로 운영하면서도, 학생에게는 실제 실험에서 보이지 않는 내부 현상과 열전달량을 정성적·정량적으로 함께 보여줄 수 있다.
